import * as store from '../../../../server/commerce';
export const dynamic='force-dynamic';
export async function GET(request:Request,{params}:{params:Promise<{action:string}>}){return store.safe(async()=>{const {action}=await params;if(action==='catalog')return store.json({products:await store.publicCatalog()});if(action==='configuration')return store.configuration(request);if(action==='purchases')return store.purchases(request);if(action==='sales')return store.sales(request);return store.json({error:'Not found'},404);});}
export async function POST(request:Request,{params}:{params:Promise<{action:string}>}){return store.safe(async()=>{const {action}=await params;if(action==='create')return store.createOrder(request);if(action==='capture')return store.captureOrder(request);if(action==='webhook')return store.webhook(request);if(action==='download-token')return store.downloadToken(request);if(action==='download')return store.download(request);if(action==='owner-upload')return store.uploadOwnerFile(request);if(action==='owner-file')return store.ownerResourceFile(request);if(action==='provision'){store.mutation(request);const user=await store.customer(request);if(!store.owner(user))return store.json({error:'Owner access required'},403);return store.json(await store.provision());}return store.json({error:'Not found'},404);});}
